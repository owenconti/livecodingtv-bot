//'use strict'

//const Settings = require('../utils/Settings');
//const connectMessage = Settings.getSetting( __filename, 'connectMessage' );
//const Templater = require('../utils/Templater');
//const Say = require('../utils/Say');

//module.exports = [{
    //types: ['presence'],
    //regex: /^available$/,
    //action: function( chat, stanza ) {
		//let msg = Templater.run( connectMessage, {
			//username: stanza.user.username
		//} );
        //setTimeout(() => {
          //Say.say( stanza.user.username + ' connected bra.' )
        //}, 1000);
    //}
//}];
